% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cuda_prebuilt.R
\name{pjrt_cuda_build_kernels}
\alias{pjrt_cuda_build_kernels}
\title{Build Prebuilt CUDA Kernels}
\usage{
pjrt_cuda_build_kernels(package, dest = ".", targets = cuda_default_targets())
}
\arguments{
\item{package}{(\code{character(1)})\cr
The package, which must be installed.}

\item{dest}{(\code{character(1)})\cr
The directory to write the tarball to.}

\item{targets}{(\code{character()})\cr
What to compile each module for: \code{"sm_XY"} makes a cubin for GPUs of
compute capability X.Y (which also runs on later X.* ones), and
\code{"compute_XY"} PTX, which the driver compiles for any later GPU.}
}
\value{
(\code{character(1)})\cr
The path of the tarball, invisibly.
}
\description{
Compiles the CUDA modules a package ships ahead of time, into the tarball
that pjrt downloads in place of compiling them on the user's machine.

The modules are the ones the package creates with
\code{pjrt_cuda_module(..., package = pkgname)} when it is loaded, so they are
built exactly as they are used. Compiling needs NVRTC, from the CUDA R
package the CUDA plugin uses, but no GPU.

Attach the tarball to the package's release, and name its URL in the
package's DESCRIPTION field \code{Config/pjrt/cuda-kernels}, where \code{{version}}
stands for the package version, e.g.
\verb{https://github.com/org/pkg/releases/download/v\{version\}/pkg-cuda-kernels-\{version\}.tar.gz}.
}
