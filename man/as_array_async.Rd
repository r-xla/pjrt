% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buffer.R
\name{as_array_async}
\alias{as_array_async}
\title{Convert buffer to R array asynchronously}
\usage{
as_array_async(x, ...)
}
\arguments{
\item{x}{A \code{PJRTBuffer} object.}

\item{...}{Additional arguments (unused).}

\item{integer64}{(\code{logical(1)})\cr
If \code{TRUE} and the buffer dtype is \code{"i64"}, the materialized result will
be a \code{\link[bit64:bit64-package]{bit64::integer64}} vector (full 64-bit range)
instead of an R \code{integer} vector with a truncating cast. Requires the
\code{bit64} package. No-op for other dtypes. Defaults to \code{FALSE}.}
}
\value{
A \code{PJRTArrayPromise} object. Call \code{value()} to get the R array.
}
\description{
Start an asynchronous transfer of buffer data from device to host.
Returns immediately with a \code{PJRTArrayPromise} object.

Use \code{value()} to get the R array (blocks if not ready).
Use \code{is_ready()} to check if transfer has completed (non-blocking).
}
\examples{
\dontshow{if (plugins_downloaded()) withAutoprint(\{ # examplesIf}
buf <- pjrt_buffer(c(1.0, 2.0, 3.0, 4.0), shape = c(2, 2), dtype = "f32")
result <- as_array_async(buf)
is_ready(result)
value(result)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=as_array]{as_array()}}, \code{\link[=value]{value()}}, \code{\link[=is_ready]{is_ready()}}, \code{\link[=pjrt_execute]{pjrt_execute()}}, \code{\link[=await]{await()}}
}
