% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree.R
\name{tree_hash}
\alias{tree_hash}
\title{Structural Tree Hash}
\usage{
tree_hash(tree)
}
\arguments{
\item{tree}{(\code{RTree})\cr
A tree as returned by \code{\link[=build_tree]{build_tree()}}.}
}
\value{
\code{character(1)}, the structural hash.
}
\description{
A hash of a tree's structure -- node kinds, child structure, leaf positions,
and names -- consistent with \code{\link[=tree_equal]{tree_equal()}}: trees that are \code{\link[=tree_equal]{tree_equal()}}
hash to the same value. Intended as a cache key for tree-structured
dispatch.
}
\examples{
tree_hash(build_tree(list(a = 1))) == tree_hash(build_tree(list(a = 2)))
tree_hash(build_tree(list(a = 1))) == tree_hash(build_tree(list(b = 1)))
}
\seealso{
\code{\link[=tree_equal]{tree_equal()}}, \code{\link[=build_tree]{build_tree()}}
}
