% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buffer.R
\name{as_array.PJRTBuffer}
\alias{as_array.PJRTBuffer}
\title{Convert a PJRTBuffer to an R Array}
\usage{
\method{as_array}{PJRTBuffer}(x, integer64 = FALSE, ...)
}
\arguments{
\item{x}{(\code{\link[=pjrt_buffer]{PJRTBuffer}})\cr
Buffer to convert.}

\item{integer64}{(\code{logical(1)})\cr
If \code{TRUE} and the buffer dtype is \code{"i64"}, return a
\code{\link[bit64:bit64-package]{bit64::integer64}} vector instead of casting to R's
32-bit \code{integer}. Requires the \code{bit64} package. No-op for other dtypes.
Defaults to \code{FALSE}.}

\item{...}{Additional arguments (unused).}
}
\value{
An R \code{array} (or \code{vector} for shape \code{integer()}).
}
\description{
Transfer buffer data from device to host and return an R array.

R has no native 64-bit integer type. By default an \code{i64} buffer is
materialized into an R \code{integer} (32-bit) vector with a silent truncating
cast. Pass \code{integer64 = TRUE} to instead return a \code{bit64::integer64}
vector (a \code{REALSXP} carrying the int64 bit pattern), which round-trips
the full 64-bit range.
}
