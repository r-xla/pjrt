% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree.R
\name{tree_diff}
\alias{tree_diff}
\title{Difference Between Trees}
\usage{
tree_diff(a, b)
}
\arguments{
\item{a, b}{(\code{PJRTNode})\cr
Trees to compare, as returned by \code{\link[=build_tree]{build_tree()}}.}
}
\value{
\code{NULL} if \code{a} and \code{b} are structurally identical, otherwise a list
with elements \code{prefix}, \code{a}, and \code{b}.
}
\description{
Walks two trees in parallel and returns the first path/subtree pair where
they diverge, or \code{NULL} if they are structurally identical. The returned
\code{prefix} follows \code{\link[=tree_path]{tree_path()}} syntax; \code{a} and \code{b} are the \code{\link[=tree_repr]{tree_repr()}}
strings of the diverging subtrees.
}
\examples{
tree_diff(build_tree(list(a = 1)), build_tree(list(a = list(1, 2))))
}
\seealso{
\code{\link[=build_tree]{build_tree()}}, \code{\link[=tree_path]{tree_path()}}, \code{\link[=tree_equal]{tree_equal()}}
}
