% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/async.R
\name{pjrt_buffer_promise}
\alias{pjrt_buffer_promise}
\title{Create a PJRT Buffer Promise (internal)}
\usage{
pjrt_buffer_promise(buffer)
}
\arguments{
\item{buffer}{A PJRTBuffer external pointer (valid immediately).}
}
\value{
A \code{PJRTBufferPromise} object.
}
\description{
Internal constructor for async buffer results.
Users should not call this directly - use \code{pjrt_execute()} or
\code{pjrt_buffer()} instead.

A \code{PJRTBufferPromise} is a \code{PJRTBuffer} XPtr with an additional class.
The buffer is valid immediately and can be used in subsequent operations
(PJRT handles dependencies internally). Call \code{value()} to block until
the operation is complete.
}
\keyword{internal}
