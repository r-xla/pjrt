% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree.R
\name{tree_path}
\alias{tree_path}
\title{Tree Path}
\usage{
tree_path(node, i)
}
\arguments{
\item{node}{(\code{PJRTNode})\cr
A tree as returned by \code{\link[=build_tree]{build_tree()}}.}

\item{i}{(\code{integer(1)})\cr
The flat index of the leaf.}
}
\value{
A scalar \code{character} string (\code{""} for a leaf at the root).
}
\description{
Returns the human-readable path for a single leaf identified by its flat
index (e.g. \code{"a$b"} or \code{"[[2]]"}). Only descends into the branch containing
the target leaf, making it efficient for error reporting.
}
\examples{
tree_path(build_tree(list(a = 1, b = list(c = 2))), 2L)
}
\seealso{
\code{\link[=build_tree]{build_tree()}}, \code{\link[rlang:flatten]{flatten()}}
}
