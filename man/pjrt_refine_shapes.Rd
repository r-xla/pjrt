% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize.R
\name{pjrt_refine_shapes}
\alias{pjrt_refine_shapes}
\title{Concretize the Shapes of a StableHLO Program}
\usage{
pjrt_refine_shapes(program, types)
}
\arguments{
\item{program}{(\code{PJRTProgram} | \code{character(1)})\cr
The program to refine, either a \code{PJRTProgram} in \code{"mlir"} format or MLIR
source code.}

\item{types}{(\code{list()})\cr
One entry per argument of \code{main}, each a \code{list(dtype, shape)}:
\itemize{
\item \code{dtype} -- anything \code{\link[tengen:as_dtype]{tengen::as_dtype()}} accepts, e.g. \code{"f32"}.
\item \code{shape} -- the axis sizes, e.g. \code{c(5, 3)}, or \code{integer()} for a scalar.
}

Entries may be positional or named, so
\code{list("f32", c(5, 3))} and \code{list(dtype = "f32", shape = c(5, 3))} are the
same. Every axis size has to be concrete: a dynamic one is what this
function refines away.}
}
\value{
\code{PJRTProgram}
}
\description{
Replace the argument types of a program's \code{main} function with concrete ones
and propagate the refinement through the rest of the program.

Programs exported with dynamic (polymorphic) shapes -- e.g. by
\code{jax.export} with a symbolic batch dimension -- cannot be compiled by PJRT,
because XLA needs static shapes. Refining them turns such a program into an
ordinary, compilable one.
}
\examples{
\dontrun{
path <- system.file("programs/jax-mlp-dynamic.mlir", package = "pjrt")
program <- pjrt_program(path = path)
pjrt_refine_shapes(
  program,
  list(
    list("f32", c(3, 4)),
    list("f32", 4),
    list("f32", c(4, 2)),
    list("f32", 2),
    list("f32", c(5, 3))
  )
)
}
}
