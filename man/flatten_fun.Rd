% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree.R
\name{flatten_fun}
\alias{flatten_fun}
\title{Flatten a Function}
\usage{
flatten_fun(f, ..., in_node = NULL)
}
\arguments{
\item{f}{(\code{function})\cr
Function to wrap.}

\item{...}{Example arguments whose structure the flat inputs follow.
Mutually exclusive with \code{in_node}.}

\item{in_node}{(\code{NULL} | \code{PJRTNode})\cr
The input structure, if already available.}
}
\value{
A \code{FlattenedFunction}.
}
\description{
Wraps a function so it can be called with the flat leaf list of its
original (nested) arguments. The wrapper unflattens the leaves back into
the original argument structure, calls \code{f}, and returns
\code{list(build_tree(out), flatten(out))}.
}
\examples{
f <- function(x) list(sum = x$a + x$b)
ff <- flatten_fun(f, list(a = 1, b = 2))
ff(3, 4)
}
\seealso{
\code{\link[=build_tree]{build_tree()}}, \code{\link[=flatten]{flatten()}}, \code{\link[=unflatten]{unflatten()}}
}
