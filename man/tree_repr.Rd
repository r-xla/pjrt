% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree.R
\name{tree_repr}
\alias{tree_repr}
\title{Canonical Tree Representation}
\usage{
tree_repr(x)
}
\arguments{
\item{x}{(\code{PJRTNode})\cr
A tree as returned by \code{\link[=build_tree]{build_tree()}}.}
}
\value{
A scalar \code{character} string.
}
\description{
A canonical structural string for a tree: \code{"*"} for a leaf, \code{"NULL"} for an
empty node, and \code{"list(...)"} for list nodes. Two trees are structurally
equal iff their representations are equal, so the string can serve as a
value-semantics cache key for a tree.
}
\examples{
tree_repr(build_tree(list(a = 1, b = list(2, NULL))))
}
