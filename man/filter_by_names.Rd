% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree.R
\name{filter_by_names}
\alias{filter_by_names}
\title{Filter a Tree by Top-Level Names}
\usage{
filter_by_names(tree, names)
}
\arguments{
\item{tree}{(\code{PJRTNode})\cr
A named list tree as returned by \code{\link[=build_tree]{build_tree()}}.}

\item{names}{(\code{character()})\cr
Names of children to keep.}
}
\value{
A \code{PJRTNode} containing only the selected children.
}
\description{
Keeps only the top-level children whose names match \code{names} (in tree
order), reindexing the remaining leaves so they map to contiguous positions
in a flat list.
}
\examples{
x <- list(a = 1, b = 2, c = 3)
sub <- filter_by_names(build_tree(x), c("a", "c"))
tree_size(sub)
unflatten(sub, x[c("a", "c")])
}
\seealso{
\code{\link[=build_tree]{build_tree()}}, \code{\link[=unflatten]{unflatten()}}
}
