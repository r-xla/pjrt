% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree.R
\name{build_tree}
\alias{build_tree}
\title{Build Tree}
\usage{
build_tree(x)
}
\arguments{
\item{x}{(any)\cr
Object whose structure to capture. Bare lists are recursed into;
everything else is a leaf.}
}
\value{
A \code{RTree}.
}
\description{
Captures the nesting structure of an object as an opaque \code{RTree}.
Each leaf in the input corresponds to its position in the flat list produced
by \code{\link[=flatten]{flatten()}}. The resulting tree can be passed to \code{\link[=unflatten]{unflatten()}} to
reconstruct the original structure from a flat list.

\code{NULL} is an empty node: it holds no leaf and contributes nothing to the
flat list, but is preserved in the tree (so \code{list(x, NULL)} and
\code{list(NULL, x)} have distinct trees).

The \code{RTree} and its operations are pjrt's R analog of
\href{https://docs.jax.dev/en/latest/pytrees.html}{JAX's pytree}, which is
where the idea comes from.
}
\examples{
x <- list(a = 1, b = list(c = 2, d = 3))
tree <- build_tree(x)
tree_size(tree)

unflatten(tree, flatten(x))
}
\seealso{
\code{\link[=flatten]{flatten()}}, \code{\link[=unflatten]{unflatten()}}, \code{\link[=tree_size]{tree_size()}}
}
