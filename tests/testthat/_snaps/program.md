# load a test program

    Code
      print(program)
    Output
      PJRTProgram(format=hlo, code_size=158)
      name: "x*x.3"
      entry_computation_name: "x*x.3"
      computations {
        name: "x*x.3"
        instructions {
      ... 

